<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="App.Widget.LikeButton" parent="Widget.Material3.Button.IconButton">
        <item name="buttonTint">@color/color_like_selector</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
    </style>

    <style name="App.Widget.ShareButton" parent="Widget.Material3.Button.IconButton">
        <item name="buttonTint">@color/color_share_selector</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
    </style>

    <style name="App.Widget.MenuButton" parent="Widget.Material3.Button.IconButton">
        <item name="buttonTint">#000000</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
    </style>

    <style name="App.Widget.CheckButton" parent="Widget.Material3.Button.IconButton">
        <item name="buttonTint">@color/color_check_selector</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
    </style>

    <style name="Widget.AppTheme.IconButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="iconTint">@color/icon_button_tint</item>
        <item name="iconPadding">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
    </style>

    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
</resources>